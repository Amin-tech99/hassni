{
  "deploy": {
    "startCommand": "./prestart.sh && python -m gunicorn main:app --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
